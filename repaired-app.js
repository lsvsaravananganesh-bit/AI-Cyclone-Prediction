const input=document.getElementById('file');const preview=document.getElementById('preview');const result=document.getElementById('result');if(input){input.addEventListener('change',()=>{const file=input.files&&input.files[0];if(!file)return;if(!file.type.startsWith('image/')){result.innerHTML='<h4>Invalid file</h4><p>Please select a satellite image.</p>';return}const url=URL.createObjectURL(file);preview.innerHTML='<img src="'+url+'" alt="Uploaded satellite image" style="max-width:100%;max-height:260px;border-radius:12px;object-fit:contain"><strong>'+file.name+'</strong><small>Image loaded locally · no real ML inference connected</small>';result.innerHTML='<h4>Image received</h4><p>The upload pipeline is working. Connect the trained model endpoint to produce a real prediction.</p><ul><li>Input validation: passed</li><li>Model status: not loaded</li><li>Prediction: not generated</li></ul>'})}