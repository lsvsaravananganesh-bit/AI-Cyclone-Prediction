const DEMO={cyclone_detected:true,cyclone_name:'VAYU (DEMO)',confidence:.92,pattern:'Organized Spiral',stage:'Mature Cyclone',category:'Severe Cyclonic Storm',latitude:15.2,longitude:84.1,wind_speed:110,pressure:980,risk_score:72};
const TRACK=[[15.2,84.1],[15.7,83.6],[16.2,83.1],[17.1,82.4],[18.0,81.6],[18.8,80.7],[19.3,80.6]];
const FORECAST=[['NOW',110,980,92],['+6H',115,978,88],['+12H',122,975,84],['+24H',130,970,80],['+48H',120,972,76]];
const HISTORICAL=[
 {name:'Fani',year:2019,category:'Extremely Severe',wind:215,match:92},
 {name:'Amphan',year:2020,category:'Super Cyclonic',wind:260,match:88},
 {name:'Yaas',year:2021,category:'Very Severe',wind:150,match:76},
 {name:'Tauktae',year:2021,category:'Very Severe',wind:185,match:69},
 {name:'Biparjoy',year:2023,category:'Very Severe',wind:195,match:64}
];
const API_BASE=window.CYCLONE_API_BASE||'';
let map,marker,aiLine,aiCone,chart,playing=false,timer;
const $=id=>document.getElementById(id),txt=(id,v)=>$(id)&&($(id).textContent=v),risk=d=>d.risk_score??Math.round(Math.min(98,Math.max(10,(+d.wind_speed||0)*.45+(1015-(+d.pressure||1015))*.4+(+d.confidence||0)*18)));
function setPrediction(d,mode='DEMO FALLBACK'){
 const c=Math.round((+d.confidence||0)*100),lat=+d.latitude||DEMO.latitude,lon=+d.longitude||DEMO.longitude,w=+d.wind_speed||DEMO.wind_speed,p=+d.pressure||DEMO.pressure;
 txt('status',d.cyclone_detected===false?'No':'Yes');txt('confidence',c+'%');txt('pattern',d.pattern||'Organized Spiral');txt('stage',d.stage||'Mature Cyclone');txt('windTop',w+' km/h');txt('categoryTop','Category: '+(d.category||DEMO.category));txt('locationTop',lat.toFixed(1)+'° N, '+lon.toFixed(1)+'° E');txt('mapCurrent',lat.toFixed(1)+'°N, '+lon.toFixed(1)+'°E');txt('mapConfidence',c+'%');txt('detectResult',d.cyclone_detected===false?'No Cyclone Detected':'Cyclone Detected');txt('detectConf','Confidence: '+c+'%');txt('patternResult',d.pattern||'Organized Spiral');txt('patternResultStage','Stage: '+(d.stage||'Mature Cyclone'));txt('intensityResult',w+' km/h');txt('pressureResult','~'+p+' hPa');txt('predCyclone',d.cyclone_detected===false?'Not detected':'Detected');txt('predPattern',d.pattern||'Organized Spiral');txt('predIntensity',w+' km/h');txt('predStage',d.stage||'Mature Cyclone');txt('predConfidence',c+'%');txt('predPressure','~'+p+' hPa');txt('envWind',w+' km/h');txt('envPressure',p+' hPa');txt('riskScore',risk(d));txt('predictionMode',mode);txt('lastUpdate','Last updated: '+new Date().toLocaleTimeString('en-IN',{hour12:false})+' IST');
 if(marker){marker.setLatLng([lat,lon]);if(map)map.setView([lat,lon],5);}
 if(aiLine){const shift=lat-DEMO.latitude,lonShift=lon-DEMO.longitude;const points=TRACK.map((x,i)=>[x[0]+shift,x[1]+lonShift]);aiLine.setLatLngs(points);aiCone.setLatLngs(corridor(points));}
 renderTimeline(w);
}
function corridor(points){const a=[],b=[];points.forEach((x,i)=>{const width=.12+i*.055;a.push([x[0]+width,x[1]-width]);b.unshift([x[0]-width,x[1]+width])});return a.concat(b)}
function stormIcon(){return L.divIcon({className:'',html:'<span class="storm-marker"></span>',iconSize:[18,18],iconAnchor:[9,9]})}
function initMap(){if(!window.L||!$('cycloneMap'))return;map=L.map('cycloneMap',{zoomControl:false,worldCopyJump:true}).setView([16.5,82.5],5);const sat=L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:18,attribution:'Tiles © Esri'});sat.addTo(map);L.control.zoom({position:'bottomright'}).addTo(map);aiLine=L.polyline(TRACK,{color:'#ffbd38',weight:4,dashArray:'9 7'}).addTo(map);aiCone=L.polygon(corridor(TRACK),{color:'#45aaff',weight:1,dashArray:'5 5',fillColor:'#3b9fff',fillOpacity:.08}).addTo(map);marker=L.marker(TRACK[0],{icon:stormIcon(),zIndexOffset:1000}).addTo(map).bindPopup('<b>AI Cyclone Prediction</b><br>Prototype prediction center<br>Not an operational warning');TRACK.slice(1).forEach((p,i)=>L.circleMarker(p,{radius:5,color:'#ffbd38',fillColor:'#ffbd38',fillOpacity:1,weight:1}).bindTooltip('T+'+((i+1)*12)+'h • forecast').addTo(map));$('centerStorm').onclick=()=>marker&&map.setView(marker.getLatLng(),6,{animate:true});$('toggleAnimation').onclick=play;window.addEventListener('resize',()=>map&&map.invalidateSize());setTimeout(()=>map.invalidateSize(),250)}
function play(){if(playing){clearInterval(timer);playing=false;txt('toggleAnimation','▶ PLAY');return}playing=true;let i=0;txt('toggleAnimation','■ STOP');timer=setInterval(()=>{const p=TRACK[i];if(marker)marker.setLatLng(p);if(map)map.panTo(p,{animate:true,duration:.3});i++;if(i>=TRACK.length){clearInterval(timer);playing=false;txt('toggleAnimation','▶ PLAY')}},700)}
function initChart(){if(!window.Chart||!$('forecastChart'))return;chart=new Chart($('forecastChart'),{type:'line',data:{labels:FORECAST.map(x=>x[0]),datasets:[{label:'Wind km/h',data:FORECAST.map(x=>x[1]),borderColor:'#ff4e4e',backgroundColor:'#ff4e4e22',fill:true,tension:.35,pointRadius:3},{label:'Pressure hPa',data:FORECAST.map(x=>x[2]),borderColor:'#3e8cff',tension:.35,yAxisID:'p',pointRadius:3}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:'#8da6b2',font:{size:8}}}},scales:{x:{grid:{color:'#173342'},ticks:{color:'#647e8a',font:{size:8}}},y:{grid:{color:'#173342'},ticks:{color:'#ff8a72',font:{size:8}}},p:{position:'right',grid:{drawOnChartArea:false},ticks:{color:'#57a6ff',font:{size:8}}}}}})}
function renderTimeline(w=110){const values=[w,Math.round(w*1.04),Math.round(w*1.1),Math.round(w*1.18),Math.round(w*1.09)];txt('predictionTimeline','');const box=$('predictionTimeline');if(box)box.innerHTML=values.map((v,i)=>`<div><small>${FORECAST[i][0]}</small><b>${v} km/h</b><span>${FORECAST[i][3]}% conf.</span></div>`).join('')}
function renderHistorical(){const box=$('historicalRows');if(!box)return;box.innerHTML=HISTORICAL.map(h=>`<div class="history-row"><strong>◉ ${h.name} (${h.year})</strong><span class="badge">${h.category}</span><span>${h.wind} km/h</span><span class="match">${h.match}% similar</span><button class="view-btn" data-history="${h.name}">View</button></div>`).join('');box.querySelectorAll('[data-history]').forEach(b=>b.onclick=()=>{const h=HISTORICAL.find(x=>x.name===b.dataset.history);if(h)txt('aiReason',`Historical comparison: the current image pattern has a ${h.match}% similarity score with ${h.name} (${h.year}). This is a prototype comparison, not a scientific reanalysis.`)})}
function getUploadHistory(){try{return JSON.parse(localStorage.getItem('cycloneAIHistory')||'[]')}catch{return[]}}
function saveUpload(entry){const history=getUploadHistory();history.unshift(entry);localStorage.setItem('cycloneAIHistory',JSON.stringify(history.slice(0,8)));renderUploadHistory()}
function renderUploadHistory(){const box=$('uploadHistoryList'),history=getUploadHistory();txt('historyCount',history.length+' ANALYSES');if(!box)return;if(!history.length){box.innerHTML='<div class="empty-history">No uploaded images analyzed yet.<br><span>Upload a satellite image above to create a history entry.</span></div>';return}box.innerHTML=history.map((h,i)=>`<div class="history-entry"><img src="${h.image}" alt=""><div><b>${escapeHtml(h.name)}</b><span>${h.time} • ${h.pattern}</span><em>${h.wind} km/h • ${h.confidence}% confidence</em></div><button class="view-btn" data-entry="${i}">Load</button></div>`).join('');box.querySelectorAll('[data-entry]').forEach(b=>b.onclick=()=>loadHistory(+b.dataset.entry))}
function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function loadHistory(i){const h=getUploadHistory()[i];if(!h)return;$('uploadPreviewWrap').hidden=false;$('uploadPreview').src=h.image;$('uploadName').textContent=h.name;$('uploadMeta').textContent=h.meta;setPrediction(h,h.mode||'HISTORY');$('satPreview').src=h.image;txt('imageTime',h.time);txt('aiReason',h.reason||'Loaded from local analysis history.');location.hash='upload'}
function analyzeDemo(file){const seed=(file.size%31)+(file.width||0)%17;const wind=95+(seed%46);const conf=.78+(seed%17)/100;const patterns=['Organized Spiral','Curved Band','Disorganized Cluster','Developing Eye'];const pattern=patterns[seed%patterns.length];const stage=wind>=130?'Very Severe Cyclonic Storm':wind>=118?'Severe Cyclonic Storm':wind>=90?'Cyclonic Storm':'Depression';const lat=14.8+(seed%9)*.18,lon=82.7-(seed%8)*.22;return{cyclone_detected:true,pattern,stage,category:stage,confidence:conf,wind_speed:wind,pressure:Math.round(1002-(wind-90)*.42),latitude:lat,longitude:lon,risk_score:Math.round(45+conf*20+(wind-90)*.42),reason:`The demo classifier found a ${pattern.toLowerCase()} signature. Cloud organization, cold cloud-top regions and the image's structural pattern are used as placeholders until the trained ML model is connected.`}}
async function runImagePrediction(file){if(!file)return;if(file.size>10*1024*1024){alert('Please upload an image smaller than 10 MB.');return}const reader=new FileReader();reader.onload=async e=>{const image=e.target.result;$('uploadPreviewWrap').hidden=false;$('uploadPreview').src=image;$('uploadName').textContent=file.name;$('uploadMeta').textContent=(file.type||'image')+' • '+(file.size/1024/1024).toFixed(2)+' MB';txt('satStatus','UPLOADED IMAGE');txt('imageTime','Uploaded '+new Date().toLocaleString('en-IN'));let data,mode='DEMO FALLBACK';try{if(API_BASE){const form=new FormData();form.append('image',file);const r=await fetch(API_BASE+'/api/predict-image',{method:'POST',body:form});if(r.ok){data=await r.json();mode='ML API CONNECTED'}}}catch(err){console.warn('ML API unavailable; using demo fallback',err)}if(!data)data=analyzeDemo(file);setPrediction(data,mode);$('satPreview').src=image;txt('aiReason',data.reason);saveUpload({name:file.name,time:new Date().toLocaleString('en-IN'),meta:(file.type||'image')+' • '+(file.size/1024/1024).toFixed(2)+' MB',image,pattern:data.pattern||'Unknown',wind:data.wind_speed||'—',confidence:Math.round((data.confidence||0)*100),mode,reason:data.reason})};reader.readAsDataURL(file)}
function initUpload(){const input=$('imageInput'),zone=$('dropzone');if(!input)return;input.onchange=()=>input.files[0]&&runImagePrediction(input.files[0]);['dragenter','dragover'].forEach(ev=>zone.addEventListener(ev,e=>{e.preventDefault();zone.classList.add('drag') }));['dragleave','drop'].forEach(ev=>zone.addEventListener(ev,e=>{e.preventDefault();zone.classList.remove('drag')}));zone.addEventListener('drop',e=>{const f=e.dataTransfer.files[0];if(f)runImagePrediction(f)});$('analyzeImage')?.addEventListener('click',()=>input.files[0]&&runImagePrediction(input.files[0]));$('analyzeImageTop')?.addEventListener('click',()=>{location.hash='upload';input.click()});$('clearHistory')?.addEventListener('click',()=>{localStorage.removeItem('cycloneAIHistory');renderUploadHistory()})}
function clock(){const d=new Date;txt('clock',d.toLocaleTimeString('en-IN',{hour12:false,timeZone:'Asia/Kolkata'})+' IST');txt('dateClock',d.toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric',timeZone:'Asia/Kolkata'}))}
function start(){setPrediction(DEMO);renderTimeline();renderHistorical();renderUploadHistory();initMap();initChart();initUpload();clock();setInterval(clock,1000);$('refresh')?.addEventListener('click',()=>{txt('lastUpdate','Refreshing…');setTimeout(()=>txt('lastUpdate','Last updated: '+new Date().toLocaleTimeString('en-IN',{hour12:false})+' IST'),400)})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();